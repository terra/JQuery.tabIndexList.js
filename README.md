JQuery.tabIndexList.js
======================

A jQuery plugin that adds a static 'tabIndexList' method to define the order of tabIndex elements in the page.

Using
-----
Get de js file at releases folder and add in your page.
